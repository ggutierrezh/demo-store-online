spring:
  application:
    name: customer-service
    
  cloud:
    config:
      uri:
      - http://localhost:8081
      username: root
      password: s3cr3t
  
 # Conexión al Admin Server    
  boot:
    admin:
      client:
        url: http://localhost:8097
        instance:
          management-base-url: http://localhost:8097
